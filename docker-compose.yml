services:
  qdrant:
    image: qdrant/qdrant:1.19.1
    container_name: qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: always

volumes:
  qdrant_storage:
    name: qdrant_storage
